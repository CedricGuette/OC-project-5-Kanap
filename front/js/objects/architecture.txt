Product => {
     JSON   => cree un objet
            => renvoie informations sur l'objet 
            
} SEEMS LEGIT


C'est pareil mdr
Cart => {
    recoit des produits => ajoute au panier
                        => modifie le panier
                        => supprime objet du panier
                        => renvoie informations sur le panier

LocalStorage => 
    recoit un array => renvoie un array
                    => met à jour le localStorage
                    => structure l'array
                    => permet d'acceder aux cellules

} SEEMS LEGIT

Price => {
    recoit un array => calcul le prix du panier
                    => 
}



}

Api => {
    call fetch  => renvoie liste d'objet
                => renvoie un seul Object
                => post la commande
} 


Order => {
    recoit deux array => structure le post
}